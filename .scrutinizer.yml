filter:
  excluded_paths:
    - debug/
  dependency_paths:
    - wcf/
checks:
  php:
    code_rating: true
    duplication: true
build:
  environment:
    php:
      version: 7.3
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run