build:
        environment:
          php:
            version: 7.4
        nodes:
          analysis:
            dependencies:
              before:
                - git clone https://github.com/WoltLab/WCF.git wcf/
                - cd wcf/
                - git checkout 5.2
            tests:
              override:
                - php-scrutinizer-run
filter:
  dependency_paths:
    - wcf/