define(['Ajax', 'Ui/Dialog', 'Language', 'StringUtil'], function (Ajax, UiDialog, Language, StringUtil) {
    "use strict";

    return {
        init: function () {
            var button = elById('showUserResult');
        },
    };
});
